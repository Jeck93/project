[build]
  publish = "pwa-app-KB"

[context.production.environment]
  NODE_ENV = "production"

# Disable Netlify Identity - using custom secure authentication
[template.environment]
  NETLIFY_IDENTITY_ENABLED = "false"